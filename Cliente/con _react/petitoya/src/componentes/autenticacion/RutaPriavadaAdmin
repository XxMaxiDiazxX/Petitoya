import React from 'react';
import { useNavigate } from 'react-router-dom';
import { useAuth } from './AuthContext';

const RutaPrivadaAdmin = ({ element }) => {
  const { isLoggedIn, user } = useAuth();

  const navigate = useNavigate()

  // Verifica si el usuario está autenticado y es administrador
  if (isLoggedIn && user.isAdmin) {
    return element;
  } else {
    // Si no cumple con los requisitos, redirige a otra página (puedes cambiar esto según tus necesidades)
    return navigate("/")
  }
};

export default RutaPrivadaAdmin;

// {isLoggedIn && user.isAdmin && (
//     <Nav.Link as={Link} to='ruta-privada-admin'>Ruta Privada Admin</Nav.Link>
//   )}
// </Container>

